# Forensic Metadata Analyzer - Requirements
# Core dependencies
colorama>=0.4.6
tabulate>=0.9.0

# File type detection
python-magic>=0.4.27

# Image metadata extraction
Pillow>=10.0.0

# PDF metadata extraction (at least one required)
PyPDF2>=3.0.0
pikepdf>=8.0.0

# Audio metadata extraction
mutagen>=1.46.0

# Office document metadata extraction
python-docx>=0.8.11
